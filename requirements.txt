google-genai>=2.11.0
python-dotenv>=1.0.1
PyYAML>=6.0.1
beautifulsoup4>=4.12.3
lxml>=5.2.0
pytest>=8.2.0

# Client document ingestion and retrieval
pypdf>=4.2.0
python-docx>=1.1.2
numpy>=1.24.0

# Renders the synthetic certificate scans in tools/make_scan_fixtures.py, which
# is how the OCR path is exercised without using a real client document.
Pillow>=10.3.0

# Outlook desktop integration. Windows only; the rest of the package works without it.
pywin32>=306; sys_platform == "win32"
